name: CI

on:
  push:
    branches: [main]
  pull_request:

# verify ist das Merge-Gate (Blueprint §11).
jobs:
  backend:
    name: Backend (mvn verify, inkl. Frontend-Build via Quinoa)
    # TODO Unternehmenskontext: auf self-hosted Runner (ARC) umstellen.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-java@v5.6.0
        with:
          distribution: temurin
          java-version: '25'
          cache: maven
      - uses: actions/setup-node@v7
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: webapp/package-lock.json
      - name: Build & Verify (Tests, ArchUnit, Coverage-Gate)
        run: ./mvnw -B verify

  frontend:
    name: Frontend (Vitest, ESLint, Prettier-Check)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: webapp
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: webapp/package-lock.json
      - run: npm ci
      - run: npm run format:check
      - run: npm run lint
      - run: npm test -- --watch=false

  # Der Sidecar hatte lange gar keine Tests - und genau dort steckte ein Socket-Leck,
  # das den Dienst alle paar Stunden ins Speicherlimit trieb. Der Test braucht keine
  # Fremdpakete (tinytuya und geckolib werden gestubbt), also genuegt ein nacktes Python.
  sidecar:
    name: Tuya-Sidecar (Regressionstests)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.12'
      - run: python3 tools/tuya-sidecar/test_gecko_cleanup.py

  # Das HomeKit-Plugin ist ein eigenes Deployable (laeuft in Homebridge, nicht in der App),
  # deshalb ein eigener Job - aber im selben Gate: Die Mapping-Logik lebt hier in TypeScript
  # statt in getestetem Java und braucht denselben Schutz.
  homebridge:
    name: HomeKit-Plugin (tsc, Vitest)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: homebridge-smarthome
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: homebridge-smarthome/package-lock.json
      - run: npm ci
      - run: npm run lint
      - run: npm test

  # E2E (Playwright gegen das gebaute Artefakt) bewusst als eigener, nicht-blockierender Job.
  # Aktivieren, sobald die App einen sinnvollen E2E-Umfang hat.
  # e2e:
  #   runs-on: ubuntu-latest
  #   steps: ...
