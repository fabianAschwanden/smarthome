# Maven / Quarkus
target/
*.log

# Node
node_modules/
webapp/dist/
webapp/out-tsc/
webapp/test-results/
webapp/playwright-report/

# IDE
.idea/
*.iml
.vscode/*
!.vscode/settings.json
.settings/
.project
.classpath

# OS / sonstiges
.DS_Store
.env

# Deploy-Secrets (Heim-Server / Fly) – nur die .env.example werden eingecheckt
deploy/.env
deploy/**/.env
# WireGuard-Peer-Config (enthält private keys) – nie ins Repo
*.conf
home.conf

# Lokale Secrets (Tuya local-keys etc.) – nur die .example-Vorlage wird eingecheckt
config/application.properties

# go2rtc-Config enthält die Kamera-IP/RTSP-URL – nur die .example wird eingecheckt
deploy/go2rtc/go2rtc.yaml

# Python venv für Tuya-Diagnose (tinytuya)
.tuya-venv/
